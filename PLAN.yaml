tasks:
  - id: task-007
    title: Add string utility functions
    description: |
      Create a new module src/string_utils.py with these functions:
        - reverse(s: str) -> str: reverse a string
        - is_palindrome(s: str) -> bool: check if string is a palindrome (case-insensitive, ignore spaces)
        - word_count(s: str) -> int: count words in a string
        - truncate(s: str, max_length: int, suffix: str = '...') -> str: truncate string to max_length, adding suffix if truncated
      Add comprehensive tests in tests/test_string_utils.py
    files_touched:
      - src/string_utils.py
      - tests/test_string_utils.py
    depends_on: []
    priority: 1
    max_turns: 40
    model: sonnet
